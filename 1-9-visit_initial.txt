对于向量的访问，下面是在第一次访问时对向量进行初始化的方法，减少批量初始化的时间
借助两个额外的向量数组, from 和 to 和一个整数 top.
别访问的向量是 data[0...n-1]

top 在程序一开始初始化为0
第一次访问时，进行一下操作：
from[i] = top
to[top] = i
top++

访问元素 i 时，进行判断，确定该元素是不是被第一次访问。
如果元素已经初始化，那么 from[i] < top 并且 to[from[i]] == i
